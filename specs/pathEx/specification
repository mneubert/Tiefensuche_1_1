enrich digraph-outsorted-neighbours with

;; sorts ;

;; constants ;

functions

findAllVertices : elem Ô digraph ­ nat;


;; predicates ;

;; variables ;

;; induction ;

axioms

findAllVertices(a, @g) = 0 ;
µ b ²_v g ­ findAllVertices(a, (g +v b) +e (a --> b)) = findAllVertices(a, g) + 1;


end enrich
